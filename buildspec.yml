version: 0.2

phases:
  install:
    commands:
      - echo Instalando dependências...
      - npm install
  build:
    commands:
      - echo Fazendo build...
      - npm run build
  post_build:
    commands:
      - echo Build finalizado com sucesso!

artifacts:
  files:
    - '**/*'
  discard-paths: no